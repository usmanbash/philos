{%- comment -%}
	Section: Text and Imagery
	Description: This section dynamically displays text and images side by side,
	             with the ability to reverse their order based on a 'reversed'
	             setting from the theme editor. The section supports conditional
	             rendering of a image blocks, adapting the grid layout based on
	             the number of blocks (images) present.

	Usage: Use this section to display text and images side by side. The section
	       supports up to 4 image blocks, and the text content can be entered
	       using a rich text field.

	Accepts:
	  - Text: A rich text field for entering text content.
	  - Reversed: A checkbox that, when checked, reverses the order of the image
	              and text content, moving the image to the right side
	  - Blocks:
	    - Image_Block: A block that contains an image picker for selecting
	      an image to display alongside the text content
	    - Max_Blocks: 4

	 - Variables:
	  - is_reversed: Determines the order of image and text display.
	                True if the image is on the right, false otherwise.
	  - block_count: The number of image blocks present in the section.
	  - has_blocks: A boolean that determines if the section has image blocks.
	  - has_text: A boolean that determines if the section has text content.
	  - text_wrapper_classes: A string that contains the classes for the text wrapper.
	  - image_wrapper_classes: A string that contains the classes for the image wrapper.
{%- endcomment -%}

{% render 'text-and-imagery',
  text: section.settings.text,
  reversed: section.settings.reversed,
  dark_mode: section.settings.dark_mode,
  blocks: section.blocks
%}

{% schema %}
{
	"name": "Text and Imagery",
	"tag": "section",
	"settings": [
		{
			"type": "richtext",
			"label": "Text",
			"id": "text"
		},
		{
			"type": "checkbox",
			"label": "Reversed",
			"id": "reversed",
			"info": "Reverses the order of the image and text content, moving the image to the left side."
		},
    {
      "type": "checkbox",
      "label": "Dark Mode",
      "id": "dark_mode",
      "info": "When this field is checked, the text will be white and the background will be dark."
    }
	],
	"blocks": [
		{
			"type": "image_block",
			"name": "Image Block",
			"settings": [
				{
					"type": "image_picker",
					"label": "Image",
					"id": "image"
				}
			]
		}
	],
	"max_blocks": 4,
	"presets": [
		{
			"name": "Text and Imagery"
		}
	]
}
{% endschema %}
