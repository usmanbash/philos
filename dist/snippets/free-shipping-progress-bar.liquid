{%- comment -%}
Snippet: free-shipping-progress.liquid
Description:
Displays how much is left until free shipping and a progress bar.
Requirements:
Must be rendered inside Alpine `x-data="miniCart({...})"` context
{%- endcomment -%}

<div x-data class="w-full mt-6">
  <div class="flex justify-between items-center mb-3 text-16 font-semibold">
    <span x-text="freeShippingTitle"></span>
    <span
      x-text="freeShippingPercentageMessage"
      x-show="!freeShippingUnlocked"
      class="text-16 font-semibold"></span>
  </div>

  <div class="w-full h-[12px] bg-gray relative overflow-hidden">
    <div
      class="h-full bg-black transition-all duration-300"
      :style="`width: ${freeShippingPercentage}%`">
    </div>
  </div>
</div>
