{%- comment-%}
Snippet: Image Hero

Usage: This snippet is used to display a hero with a headline and subtitle.
  {% render 'image-hero',
    image: 'image.jpg',
    title: 'Title',
    text: 'Text'
  %}

Accepts:
  - image: The image to be displayed in the hero.
  - title: The headline to be displayed in the hero.
  - text: The text to be displayed in the hero.
{%- endcomment -%}

<div
  x-data="imageHero"
  class="image-hero w-full max-h-screen h-[120vw] sm:h-[70vw] lg:h-[40vw] flex justify-center relative py-14 items-end">
  {%- comment -%} Section Wrapper {%- endcomment -%}
  <div class="content-wrapper">
    {%- comment -%} Background Image {%- endcomment -%}
      <div class="absolute left-0 top-0 object-cover object-top w-full h-full">
        {%-
          render 'parallax-image'
          alt: image.alt
          image: image
          className: 'h-full'
        -%}
      </div>
    {%- comment -%} Content Wrapper {%- endcomment -%}
    <div
      data-scroll
      data-scroll-call="inViewportAnimation"
      class="relative z-10 w-full flex items-center flex-col">

      {%- comment -%} title {%- endcomment -%}
      {%- if title != blank -%}
        <h1
          data-animate
          data-preset='fadeUp'
          class="text-50 sm:text-100 text-cloud font-ivar text-center">
          {{ title }}
        </h1>
      {%- endif -%}

      {%- comment -%} Text {%- endcomment -%}
      {%- if text != blank -%}
        <div
          data-animate
          data-preset='fadeUp'
          data-to='{ "delay": 0.1 }'
          class="text-16 text-cloud text-center bottom-text px-7 sm:px-0">
          {{ text }}
        </div>
      {%- endif -%}

      {%- comment -%} Logo {%- endcomment -%}
      {% comment %}
        <div
          data-animate
          data-preset='fadeUp'
          data-to='{ "delay": 0.2 }'
          class="flex justify-center basis-full mt-5 lg:mt-10">
          <div class="image-hero-logo logo--white">
            {%- render 'icon-logo' -%}
          </div>
        </div>
      {% endcomment %}

    </div>

  </div>
</div>
